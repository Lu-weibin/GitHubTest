server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/mydemo?useUnicode=yes&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root

mybatis:
  mapperLocations: classpath*:mapper/*.xml